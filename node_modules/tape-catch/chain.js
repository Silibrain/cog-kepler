var spawn = require('comandante')

spawn('browserify', ['example.js'])
  .pipe(spawn('tap-closer'))
  .pipe(spawn('smokestack', process.argv.slice(2)))
  .pipe(spawn('tap-json'))
  .pipe(spawn('json'))
  .pipe(process.stdout)
